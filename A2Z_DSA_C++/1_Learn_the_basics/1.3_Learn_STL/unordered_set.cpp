#include<iostream>
#include<unordered_set>
using namespace std;

void unordered_sets() {
    unordered_set<int> us;

    // the only difference is that it stores elements in unsorted order
    // rest all functions are similar to set
}

int main() {
    unordered_sets();

    return 0;
}