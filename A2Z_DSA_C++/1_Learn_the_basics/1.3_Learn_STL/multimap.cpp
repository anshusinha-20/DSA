#include<iostream>
#include<map>
using namespace std;

void multimaps() { // stores elements in key value order, in sorted order but the keys can be duplicate
    multimap<int, int> mmp;
    // rest all functions are similar to map
}

int main() {
    multimaps();

    return 0;
}