#include<iostream>
using namespace std;

void partition(arr, low, high)

// BRUTE
void quickSort() {

}
int main (){
    int n;
    cout << "Enter number of elements: ";
    cin >> n;

    int arr[n];

    cout << "Enter array elements: ";
    for (int i = 0; i < n; i += 1) {
        cin >> arr[i];
    }

    quickSort(arr, 0, n - 1);
    cout << 
}